# Example scripts for real time data acquisition form tracking hostst

## Installation
Protobuf must be installed for python (see instructions in protobuf/python/readme
To buil python protbuf files, run cmake as:

```bash
cmake -DPYTHON=ON ..
```
